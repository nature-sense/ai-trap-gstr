# Build directories
build/
build-*/

# Model weights — .param and .bin are small enough to commit.
# .rknn files are larger compiled outputs — regenerate from .onnx.
models/*.rknn
models/*.onnx

# Runtime outputs
crops/
*.db
*.db-wal
*.db-shm
